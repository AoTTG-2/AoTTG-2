namespace Assets.Scripts.UI.Input
{
    /// <summary>
    /// Rebind enum used for Cannons
    /// </summary>
    public enum InputCannon
    {
        Up,
        Down,
        Left,
        Right,
        Slow,
        Shoot,
        Mount //TODO: Remove this once Issue 75 is done as cannon mounting is done per interaction wheel
    }
}
