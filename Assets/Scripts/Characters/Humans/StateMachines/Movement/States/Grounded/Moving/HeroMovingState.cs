namespace Assets.Scripts.Characters.Humans.StateMachines.Movement.States.Grounded.Moving
{
    /// <summary>
    /// Moving State Class.
    /// Defines everything that is needed and can be done while moving.
    /// </summary>
    public class HeroMovingState : HeroGroundedState
    {
        public HeroMovingState(HeroMovementStateMachine heroMovementStateMachine) : base(heroMovementStateMachine)
        {
        }
    }
}
